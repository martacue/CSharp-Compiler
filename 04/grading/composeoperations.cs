namespace Programa
{
    class Main 
    { 
        public static void main () { 
        int valor = 5 + 3 * 2;
        Console.writeLine(valor);
        valor = 5 + 3 - 2;
        Console.writeLine(valor);
        valor = 6 / 2 + 2 * 2;
        Console.writeLine(valor);
        } 
    }

}